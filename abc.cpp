#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a,b,c,d;
    cout << "Nhap so a: "; cin >> a;
    cout << "\nNhap so b: "; cin >> b;
    cout << "\nNhap so c: "; cin >> c;
    cout << "\nNhap so d: "; cin >> d;
    cout << "\nBon so vua nhap la: " << a << ' ' << b << ' ' << c << ' ' << d;
    return 0;
}
